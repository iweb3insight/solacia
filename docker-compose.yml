services:
  solacia:
    build: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      - API_HOST=0.0.0.0
    volumes:
      - ./data:/app/data
    restart: unless-stopped
